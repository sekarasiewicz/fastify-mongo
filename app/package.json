{
  "name": "fastify-mongo",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --exec fastify start app.js",
    "test": "standard && tap test/*.test.js test/*/*.test.js test/*/*/*.test.js",
    "dev": "nodemon --exec fastify start -l info -P app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.0",
    "eslint-config-airbnb-base": "^13.0.0",
    "eslint-plugin-import": "^2.13.0",
    "fastify": "^1.6.0",
    "fastify-auth": "^0.3.0",
    "fastify-autoload": "^0.4.0",
    "fastify-cli": "^0.16.4",
    "fastify-jwt": "^0.3.0",
    "fastify-plugin": "^1.1.2",
    "mongoose": "^5.2.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.2",
    "eslint": "^4.16.0"
  }
}
